import { green, red } from '@mui/material/colors';
import { SxProps, Theme } from '@mui/material';

export const SX_QUICK_RED: SxProps<Theme> = {
  color: red[500],
};
export const SX_QUICK_GREEN: SxProps<Theme> = {
  color: green[500],
};
